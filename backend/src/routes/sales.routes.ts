import { Router } from 'express';

const router = Router();

// Placeholder - implementar CRUD de vendas
router.get('/', async (_req, res) => {
  res.json({ status: 'success', message: 'Sales routes - to be implemented' });
});

export default router;
